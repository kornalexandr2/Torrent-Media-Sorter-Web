[PATHS]
; Пути назначения для файлов. Сюда будут перемещены скачанные медиа.
movies_folder = /mnt/media/movies
series_folder = /mnt/media/tvshows

[LOGGING]
; Уровень логирования: DEBUG, INFO, WARNING, ERROR
level = INFO
; Путь к файлу логов. Убедитесь, что у пользователя есть права на запись.
log_file = /opt/mediasorter/sorter.log

[SYSTEM]
; Расширения файлов, которые скрипт будет обрабатывать как видео
video_extensions = .mkv,.avi,.mp4,.mov,.wmv,.m4v,.ts

[RENAMING]
; Режим переименования основного названия (из API):
; ru        - Использовать русское название (если есть)
; en        - Использовать английское название (если есть)
; origin    - Использовать оригинальное название
; no_change - Не менять название (оставить как в торренте)
rename_mode = ru

; Сохранять ли оригинальное имя файла (из торрента) в скобках?
; True  -> "Название фильма (Original.File.Name).mkv"
; False -> "Название фильма.mkv"
save_original_filename = True

[API]
; --- KINOPOISK (Unofficial) ---
; https://kinopoiskapiunofficial.tech
use_kp = True
kp_api_key = YOUR_KINOPOISK_API_KEY

; --- TMDB (The Movie Database) ---
; https://www.themoviedb.org/settings/api
use_tmdb = True
tmdb_api_key = YOUR_TMDB_API_KEY

; --- TVDB (The TV Database) ---
; https://thetvdb.com/api-information
use_tvdb = False
tvdb_api_key = YOUR_TVDB_API_KEY

[TELEGRAM]
; Включить или выключить уведомления в Telegram
use_telegram = False
bot_token = YOUR_TELEGRAM_BOT_TOKEN
chat_id = YOUR_TELEGRAM_CHAT_ID

[TRANSMISSION]
; Настройки подключения к Transmission RPC для удаления торрентов после обработки
host = localhost
port = 9091
; Если настроена аутентификация, введите данные ниже
username = 
password =